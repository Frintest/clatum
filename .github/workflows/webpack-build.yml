name: Build Webpack
on: workflow_dispatch
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4.1.1

      - name: Install 📥
        run: npm ci

      - name: Build 📦
        run: npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist
          subfolder: "/"  # Укажите здесь нужный вам подкаталог
          deploy-token: ${{ secrets.GITHUB_TOKEN }}
          branch: gh-pages  # Измените на вашу целевую ветку Pages
      
      # - name: Run 🏃
      #   run: |
      #     node20 './src/public/index.js'
